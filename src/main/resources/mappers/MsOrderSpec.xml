<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dutchOrder.server.dao.MsOrderDao">

	<select id="msOrderList"
		resultType="com.dutchOrder.server.model.MsOrderSpec">
		SELECT o.*, os.osnum
		FROM ordering o
		JOIN order_spec os ON o.onum = os.osnum
		WHERE o.bnum = #{bnum}
		ORDER BY o.odate DESC;

	</select>

	<update id="msOrderSpecUpdate"
		parameterType="com.dutchOrder.server.model.MsOrderSpec">
		UPDATE ordering
		SET ostatus_mikey = #{ostatus_mikey}
		WHERE mnum = #{mnum}
	</update>

</mapper>