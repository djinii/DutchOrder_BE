<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dutchOrder.server.dao.MsShopMapper">


<!-- 	<select id="mapShopCategoryLi" parameterType="int" resultType="MsShop">
		
		SELECT sname, start_time, end_time
		FROM shop
		WHERE fcategory_mikey = #{fcategory_mikey};

	</select>
-->

	<select id="msShopList" resultType="MsShop">
		select sname, start_time, end_time,bnum
		from shop;
	</select>
</mapper>